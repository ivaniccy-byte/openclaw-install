# OpenClaw 职场版 - 离线资源目录

## 版本要求
- **Node.js**: v22+ LTS (推荐) / v24 (最佳)
- **Lossless-Claw Enhanced**: 最新版
- **Memory LanceDB Pro**: 最新版

## 目录结构

```
resources/
├── node-runtime/           # 内嵌Node.js运行时
│   └── win-x64/          # Windows x64版本
│       ├── node.exe       # Node.js v22+
│       └── *.dll          # Node.js依赖库
├── openclaw/             # OpenClaw核心 (Node.js)
│   ├── main.js           # OpenClaw入口
│   ├── config.yaml        # 配置文件
│   └── package.json      # Node.js依赖
├── lossless-claw-enhanced/ # Lossless Claw Enhanced
├── lancedb-pro/         # Memory LanceDB Pro
└── skills/              # 预封装Skill包
```

## 运行时说明

### Node.js 运行时
- OpenClaw 核心基于 Node.js 运行
- 建议打包 v22+ LTS 版本以获得最佳兼容性
- 安装包内置 Node.js 运行时，无需用户提前安装

## 安装要求

### 硬件要求
- CPU: 支持 AVX2 指令集（硬性要求）
- 内存: ≥4GB (推荐 8GB+)
- 磁盘: ≥10GB 可用空间 (SSD 推荐)

### 软件要求
- Windows 10/11 x64 (19045+)
- 无需预先安装 Node.js（内置）

## 离线构建说明

### Node.js 打包
```bash
# 下载 Node.js 22.x Windows x64
curl -L https://nodejs.org/dist/v22.12.0/node-v22.12.0-win-x64.zip -o node.zip
# 解压到 resources/node-runtime/win-x64/
```

## 依赖列表

### Node.js 依赖 (package.json)
```json
{
  "dependencies": {
    "openclaw-standalone": "3.28.0"
  }
}
```

## CI/CD 构建

在 GitHub Actions 中构建时：
1. 下载 Node.js 22.x embeddable 包
2. 所有资源打包进安装程序
3. 确保离线环境下也能正常安装
